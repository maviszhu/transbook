<div class="row">
  <h2 class="text-center">所有订单</h2>
  <hr>
  <% if !@orders.blank? %>
  <%= render :partial => "admin/orders/order_item", :collection => @orders, :as => :order %>
<% else %>
  <div class="col-md-12">
    订单记录为空。</div>
  <% end %>

</div>
